import { getTemplateImagenModel } from "firebase/ai";

export const ADMIN_EMAILS = new Set<string>([
  "snakeriveram@gmail.com", // Brian (if he will be admin)
  // Add your email(s) used for Firebase Auth sign-in:
  // 'you@yourdomain.com',
  "jefftkddan@gmail.com",
]);
